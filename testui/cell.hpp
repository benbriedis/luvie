#ifndef CELL_HPP
#define CELL_HPP

//XXX   Note ==> Cell

typedef struct {
	int row;
	float col;  //XXX will want velocity + anything else? (parameters that can evolve etc)
	float length;
} Note;


#endif

