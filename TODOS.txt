
Add extra using:
	Valgrind: Comprehensive but slow.
	ASan (AddressSanitizer): Flags out-of-bounds array usage and use-after-free errors instantly.
	UBSan (UndefinedBehaviorSanitizer): Flags integer overflows, null pointer dereferences, and other illegal operations.

Suggestions for Carla (install newest first)
=====================
1. Double click on rack or item in canvas to display custom UI if one exists.
   Failing that, raise the Custom UI to the top of the context UI.

2. Canvas - simplify somehow.
   Possible algoorithm - reduce its size by creating a binding box around current items and 
   adding 200px to each sides.

3. Improve filters in plugins: too easy to check something and forget its been checked. 
   Even remembers across sessions I think. Might be OK if were to drop filters when closing window.
   Alt: list the current filters under the search box?

Suggestions for RaySession (install newest first)
==========================
1. The true Close/stop button for the session is too small and hard to find.


FUTURE FUNCTIONALITY
====================

- Allow the Luvie DSP to send BPM and time signature changes to Jack (ie be a time master).
  Require the DSP to have its own Jack client connection.
  Make sure its ONLY being used to send BPM and time signature changes, plus maybe measure that 
     latency/sync thing at the start of the song that causes notes right at the start of a song to go missing.
  The Luvie UI should maintain its own separate Jack connection for sending transport messages (ONLY).

- Linear ramping of tempos...

- Could provide input triggers for patterns when in loop mode (useful to Ableton-style pads).

- Pianoroll and drum patterns - allow importing of patterns.

- Pianoroll and drum patterns - should support recording of patterns..

- Maybe allow samples to be loaded. Would reduce some friction, but Ardour can glue things together I guess.
  Likely to lead on to sample filters, envelopes, trimming, ...

- cf Is pitch randomisation possible for drums?

- Maybe add some "basic backups" - see DESIGN_DECISIONS.txt

- In standalone implement "Recent projects" menu.
	Probably use FL_Preferences (part of FLTK) to make it cross-platform compatible.

- Unit tests?

- Add a dark theme

- More MIDI parameters and features - see MIDI_EXTENSIONS.txt

LIMITATION
==========
Currently only one Luvie instance per Ardour will work. 
   cf checking to see if any others exist.
   Otherwise options exist I think. Look a bit involved.



DISTRO TO DOS?
==============

Think about installation of dependencies - eg 
   nlohmann/json			ADD license
Nice if we had an install script that installed everything locally (FLTK, JSON, Jack and LV2 deps)

- Add support for Debian build

- Snap, Flatpak, AppImage? - research which is best for audio plugins (if any. NB LV2 plugin install might be more complex)

- Icon installation needs to be included. Currently should work OK for all major Linux distros according to Claude.



8/3/26
------
Re time signatures and live looping mode
~~~
Do we get warning if deleting the plugin and notes have been added?
  In standalone probably want a warning too.


- The O(n²) searches in ObservableTimeline — with 10-20 tracks they're negligible, and the structure
isn't complex enough to warrant indexing yet.
- Floating-point bar comparisons — no evidence of actual bugs from this.
- The Point/Point2 naming — they serve genuinely different purposes.


cf being able to select a group of notes? cf picking the tool from the context menu?

Luvie (GUI) in window titlebar is odd... maybe Carla is adding it?

BUG? When changing chord the position of the notes change...



Shrinking pattern editor horizontally - could reorangise things a bit before resorting to cropping in smaller windows.

Maybe put horizontal scrollbar lower?
   Lighter theme scrollbar?


======================

Alternate plugin architectures:

1. Use the Ardour plugin (LV2 or Clap) primarily/entirely for saving the session.
   Communicate back using Jack.
   Would it sit in a separate process?
   Would ports look the same?
   Negative: requires Jack.
   
2. Clap apparantly supports transport commands. 
   May be able to remove Jack dependencies when used within a plugin.
   DONT THINK Ardour supports.
   
3. Extend LV2 to support transport commands.
   That way removes advantages Clap might have.

------------------------------TODOs---------------------------

1. Transport
	a. BUG: Luvie play/pause should update when the Jack transport is started or stopped by a different client.
	b. Improvement: positioning the playhead is bit approximate (possibly due to the buffers). Could it be made smoother?
	   Google talks about using pre-rolls. NB Ardour manages to achieve smoothness, but other programs can also be gittery.
	c. When no Jack connection the transport position labels don't update, but probably could.
	d. Document somehow that Ardour should be using the Jack sync rather than internal sync.
	   NB having Ardour set to Master doesn't seem to affect anything at the moment.
	e. BUG: Moving a note around on the Pattern editor moves the playhead forward a pixel or so (standalone, no jack)
	

2. Loop editor
	a. Swap between it an song editor based on displayed tab.
	   Disable swapping between them when playing.
	b. This editor needs its own tempo and time signature fields to replace those provided by the song editor.
	NOTE the pattern editor really needs a #beats field (rather than a #bars, which can be inconsistent between song editor and loop editor...
	   a bit forced though - is there a better way? Dont REALLY want to have to have a time signature field for every pattern which is probably
	   the "proper" way.
	)



ISSUE: In RaySession our transport does not seem to update (OK in Ardour)

cf Show recent projects - Probably need program settings to remember between runs...
   Ditto if I want to remember the last Save As / Export / Import directory.

TODO review the format of the JSON file
     cf a Zod-like description?
	 ** May help write migrators later **
     cf a version number?

Allow the window close to be clicked even if a popup of menu is showing.

Implement undo/redo (possible on an editor-by-editor basis? or current edit only?)
    Only enable Save & Save As if a change has been made (and not undone).

.........................

- Outputs overlay: click outside the port field doesnt register unless clicking into another field
  Should probably lose focus at this point.
- Outputs overlay - focused items don't fully defocus.
    

-------------------

Could have a "activate/deactive at bar start" option if I want cleaner enabling of patterns in the looper   

In the pattern editor reconsider the transport info 
    (BPM, time signature etc - some is likely to be confusing or not needed

Song editor playhead is being hidden when in Loop mode, maybe it should be shown and left where it is?

Maybe split the pause/pause button

Have the Loop Editor BPM follow changes in the SongEditor BPM? Allows for a smooth transition. [DONE i think...]
   Also allow editting (maybe not saving)
   
In time add a BPM=crotchet, BMP=dotted crotchet, BPM=quaver option I guess.   
FROM GOOGLE:
	Handling Beats Per Minute (BPM) in 8/8 time is generally treated as an 8th note pulse, where each 8th note gets one beat, rather than a quarter note. Because 8/8 is often used for irregular, additive rhythms (like 3+3+2 or 3+2+3), the tempo is usually set to the 8th note (
	) to maintain a steady pulse throughout changing groupings.

	DAW/Click Setting: In a Digital Audio Workstation (DAW), you would set the tempo based on the 8th note (in DAW terms) to get an accurate, consistent pulse   
  
In Pattern Editor clicking away from the numerator of bar fields doesn't lose the focus and update the data.
  Also clicking #Bars and dragging the mouse left or right changes the number of bars. Dont think I want.

The load drum map buttons need a better visual indication the buttons are pressed.
	Should be able to tab into them too.

             
TO TEST Program number: a) connect to Casio b) maybe connect to some software synth?

When I start up in standalone mode (with -v) and close the application (using the window X) I don't get my audio back.
   Seems as if Pipewire is lost somehow? I can get it back using qjackctl.

2. Add an 'expression' section at the bottom of BOTH the patterns and song editors
   that allow for rubber bands on a range of parameters.


Clicking and dragging to reorder the tracks in the song editor doesnt look great.

BUG: Not rewinding right to start (Ardour + Jack). See TRANSPORTS.txt.

....

Did I put a version number in the session files? [THINK IT MIGHT BE THERE - TO CHECK]


cf a pane resize drag between the parameter and notes grids

When 2 patterns share a channel and parameter maybe highlight in red and have a hover-over error message   
   

MINOR FEATURE: maybe a clear all button (or context menu options) for mute and/or solo buttons? (song editor, pattern editor)


cf looping and repositioning:  [NOTE there isn't necessary a problem here. Maybe ask AI.]
	using "Reset All Controllers" (Control Change #121) and "All Notes Off" (CC 120)
	and/or explicitly dealing with each notes and/or parameter...
	THINK using 120 + 121 should do the job... if a synth has extra controllers you'd expect them to be handled by 121.
	These should be issued BEFORE the new parameter messages are sent of course.



Standard pattern editor control row may be too wide? Wrap? Use < >  ?? 
   Or just require the user to grow the page?
   Or more an item or two down to the transport?

SMALL BUG: There is still a messy cursor transition in the pattern editors when hovering over the 
           transport ruler and dropping down into the label column - cursor doesn't reliably change.
           
MINOR: In time maybe adjust width of the chord name select box

MINOR BUG: when editting label text can get a danky FLTK popup appear when right clicking.
           Might happen in other edit boxes...


Conversion routine to clone a regular pattern to pianoroll? 
	The opposite way would be harder, but something could be attempted in time maybe.

Coding: 
	Move to modern?  widgetCursorHandle() 
	Chop up some long files?
	Subdirectories?


TEST 
	are solo/mute stored? Probably should be I guess  (song editor, pattern editor)
	STILL A BUG? Sometimes can't add notes to the drum editor  [MAY have been fixed]
	STILL A BUG? Sometime first track cant be set to drum editor [ONLY SAW ONCE.. maybe related to above error?]

--------


Create a shortcut mechanism and menu...  (cf dividing by editor type) 
   cf start/stop    - map to space
      open pattern  - eg map to <Enter>
      open context? - eg context button  (currently not working)
      solo? mute?
   Generally prefer to have 1 good set of bindings though...
      

//////////////////////////////////////

BUG: noticed once - showing the Luvie plugin UI from the Carla icon - the GUI didn't
     restore properly. Wrong cursor, couldn't do anything.

ISSUE: too easy to accidently add a marker to a ruler

MINOR BUG: clicking on Instrument selector highlights it as if it were a text field


-------------------------------------------



cf master volume rubberband control? Or any other global parameters?


Pipewire:
 - I think its working in "bridge mode" on top of Jack.
 - cf using pw-jack qjackctl instead ?  / pipewire-jack
 - To launch under Pipewire... try:    pw-jack luvie
 - Polling for Jack may not be necessary on Linux in time as Pipewire should always be there.
   The same may not be true for Windows and mac.

Error window when starting (invalid project file) is old fashioned.


Distro and Build:
	- Probably build .deb, .rpm, maybe .aur for Linux. 
	  AppImage is a possibility, but not that interested personally (going to need a build process for Windows and Mac eventually anyway).
			Also look at Flatpak and Snap.
	  MAYBE have a .zip or something for installation as a plugin. NB Can share libluvie.a between standalone and plugin versions.
	- Use CPack per platform to bundle
	- macOS: "CPack DragNDrop (.dmg) for the app; plugin as luvie.lv2 containing a .dylib, codesigned + notarized."
	- Windows: "CPack NSIS (or WiX) installer for the app; plugin .dll in a .lv2 bundle."

  
Create my own license...
	Also are NSM or Jack licenses required?
	JSON library. Others?
	What is Carla using? It aims to be similarly free I think...

=====================

Add Start & End markers in Song Editor and a looping function

Loop Editor maybe show a mini version of the patterns, similar to Seq24

Should we put the internal transport in a separate RT thread?

Wider initial app (so we can fit in pattern editor controls at start)

Loop editor: BPM should be a spinner.  Change so dragging up and down spins, not left and right + reduce the sensitivity

FEATURE: when the last time signature or tempo is off screen show it on the far left of the ruler and
         allow it to be editted.

Song parameter pitch bend + pattern pitch bend - get a warbling effect. Probably should multiply the values together (with appropriate normalisation) and output once.

TODO test isDirty in RaySession now

 
Add more chords and scales?
	Perhaps have a scale/chord toggle for convenience of selection... alternatively create a big popup with columns?

Maybe indicate which patterns are pianorolls (NB name could indicate)

Solo & Mute options
1. Leave as is
2. Just place on top track
3. Deal with patterns separately
- Solution needs to work for stacked and unstacked.

ISSUE: clicking on 'Loop' when on 'Song'... should leave the currently enabled patterns enabled

LHS label column of regular pattern too wide (maybe sharing with drum pattern)?
 
Loop editor - 
	Allow instrument names to be editted from here?

Song Editor
    Restrict orordering of patterns to those of the same type

Stacked:
  Maybe colour tracks differently
  Add some border around instrument name row so consecutive ones don't run together
     
Unstacked:  
  Maybe remove vertical lines through the instrument headers? Or fade them there.


