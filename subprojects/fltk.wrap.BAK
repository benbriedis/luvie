[wrap-git]
directory = fltk.dir
url = https://github.com/fltk/fltk.git
revision = head
depth = 1

# Something like this works:

#cmake -B build -DCMAKE_INSTALL_PREFIX=../../build/subprojects/fltk.dir 
cmake -B build -DCMAKE_INSTALL_PREFIX=install
cmake --build build
cd build
make install
